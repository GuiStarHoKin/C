/* #include <stdio.h>
#include <windows.h>

int main()
{
    int hour = 0;
    int min = 0;
    int time = 0;

    printf("请输入当前时间xx:xx:>");
    scanf("%d:%d", &hour, &min);
    printf("请输入定时分钟数:>");
    scanf("%d", &time);

    printf("%d分钟后的时间为%d:%d\n", time, (hour + (min+time)/60), (min+time)%60);


    system("pause");
    return 0;
} */

#include <stdio.h>
#include <windows.h>

int main()
{
    
    system("pause");
    return 0;
}