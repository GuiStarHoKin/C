#include <stdio.h>

int main()
{
    int a[3][2] = {(0,1),(2,3),(4,5)}; // 逗号表达式，取1,3,5,剩下都为0初始化
    int *p;
    p = a[0]; // a[0]是第一行的数组名,没有sizeof也没有&，数组名是首元素地址，1的地址
    printf("%d\n",p[0]); // p[0] == *(p+0)
    return 0;
}